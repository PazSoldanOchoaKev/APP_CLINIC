<?xml version="1.0" encoding="utf-8" ?>
<ctrls:PageBase 
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ctrls="clr-namespace:AppointmentManager.Resources.Controls"
             HeaderTitle="Mascotas"        
             Title="Lista de Mascotas"
             x:Class="AppointmentManager.Views.User.ProfileView">
    <StackLayout>
        <RefreshView IsRefreshing="{Binding IsRefresh, Mode=TwoWay}" Command="{Binding RefreshCommand}">

            <CollectionView ItemsSource="{Binding User}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid ColumnDefinitions="60, *">
                            <StackLayout Grid.Column="1">
                                <Label Text="{Binding Name}" StyleClass="Title"/>
                                <Label Text="{Binding Apellido}" StyleClass="Title"/>
                                <Label Text="{Binding TypeDocument }"/>
                                <Label Text="{Binding Document}"/>
                                <Label Text="{Binding Telefono}"/>
                                <Label Text="{Binding Address}"/>
                            </StackLayout>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </StackLayout>

</ctrls:PageBase>